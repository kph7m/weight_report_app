# Repository agent rules

- When a change alters the visible UI of the runnable app, capture an updated app screenshot and commit it with the implementation.
- When pushing implementation changes for review, push the committed branch updates after the screenshot has been added.
